using System;

class CalculateSum
{

    // Write a program to calculate the sum (with accuracy of 0.001): 1 + 1/2 - 1/3 + 1/4 - 1/5 + ...

    static void Main()
    {
        double first = 1;
        double denominator = 1;
        double second = 1/denominator;
        double next = 0;
        for (int i = 0; i < 1000; i++)
        {
            denominator++;
            if (denominator%2==0)
            {
                
            }
            else
        }
    }
}